---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: projectconfigs.controller-config.operator.kube-stager.io
spec:
  group: controller-config.operator.kube-stager.io
  names:
    kind: ProjectConfig
    listKind: ProjectConfigList
    plural: projectconfigs
    singular: projectconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ProjectConfig is the Schema for the projectconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          backupJobConfig:
            description: The config for the init job
            properties:
              backoffLimit:
                default: 3
                description: The backofflimit for the job. The job is allowed to fail
                  this many times
                format: int32
                type: integer
              deadlineSeconds:
                default: 600
                description: The deadline seconds for the completion of the job -
                  it will fail if it's not complete in this amount of time
                format: int32
                type: integer
              ttlSeconds:
                default: 600
                description: The TTL seconds for the job - The job will be cleaned
                  up after this time
                format: int32
                type: integer
            type: object
          cacheNamespace:
            description: CacheNamespace if specified restricts the manager's cache
              to watch objects in the desired namespace.
            type: string
          health:
            description: Health contains the controller health configuration.
            properties:
              healthProbeBindAddress:
                description: |-
                  HealthProbeBindAddress is the TCP address that the controller should bind to
                  for serving health probes
                type: string
            type: object
          initJobConfig:
            description: |-
              The config for the init job. The backofflimit defaults to 0 since retrying a half way complete failed init can
              cause unexpected results if the job is not prepared for this
            properties:
              backoffLimit:
                default: 3
                description: The backofflimit for the job. The job is allowed to fail
                  this many times
                format: int32
                type: integer
              deadlineSeconds:
                default: 600
                description: The deadline seconds for the completion of the job -
                  it will fail if it's not complete in this amount of time
                format: int32
                type: integer
              ttlSeconds:
                default: 600
                description: The TTL seconds for the job - The job will be cleaned
                  up after this time
                format: int32
                type: integer
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          leaderElection:
            description: LeaderElection determines whether to use leader election
              when starting the manager.
            type: boolean
          metadata:
            type: object
          metrics:
            description: Metrics contains the controller metrics configuration.
            properties:
              bindAddress:
                description: |-
                  BindAddress is the TCP address that the controller should bind to
                  for serving prometheus metrics
                type: string
            type: object
          migrationJobConfig:
            description: The config for the init job
            properties:
              backoffLimit:
                default: 3
                description: The backofflimit for the job. The job is allowed to fail
                  this many times
                format: int32
                type: integer
              deadlineSeconds:
                default: 600
                description: The deadline seconds for the completion of the job -
                  it will fail if it's not complete in this amount of time
                format: int32
                type: integer
              ttlSeconds:
                default: 600
                description: The TTL seconds for the job - The job will be cleaned
                  up after this time
                format: int32
                type: integer
            type: object
          sentryDsn:
            description: The DSN for Sentry if sentry is used to capture errors
            type: string
          webhook:
            description: Webhook contains the controller webhook configuration.
            properties:
              port:
                description: Port is the port that the webhook server serves at.
                type: integer
            type: object
        type: object
    served: true
    storage: true
